const delta = 250 // distance between the (0, 0) of canvas and (x, y) of the arc center
const rad = 200 // radius of arc
const count = 30 // number of lines
const lineW = 40 // width of line

export {delta, rad, count, lineW}